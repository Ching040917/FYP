[
 {
  "order": 0,
  "index": 0,
  "text": "Unique first paragraph of the edge fixture.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 1,
  "index": 1,
  "text": "Repeat me please.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 2,
  "index": 2,
  "text": "",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 3,
  "index": 3,
  "text": "",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 4,
  "index": 4,
  "text": "Unique between duplicates.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 5,
  "index": 5,
  "text": "Repeat me please.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 6,
  "index": 6,
  "text": "Adjacent repeat.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 7,
  "index": 7,
  "text": "Adjacent repeat.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 8,
  "index": 8,
  "text": "",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 9,
  "index": 9,
  "text": "This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. This is a deliberately long paragraph written to overflow the page boundary so the mapping must prove that a paragraph can span two rendered pages. ",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 10,
  "index": 10,
  "text": "True ambiguity.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 11,
  "index": 11,
  "text": "",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 12,
  "index": 12,
  "text": "True ambiguity.",
  "style_name": "Normal",
  "heading_level": null
 },
 {
  "order": 13,
  "index": 13,
  "text": "The final draft's figure - often quoted - works fine. Café, naïve, file, efficient.",
  "style_name": "Normal",
  "heading_level": null
 }
]